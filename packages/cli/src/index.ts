/**
 * knotrust — the CLI entry point package, published to npm as `knotrust`. Bundles the runner (`knotrust -- <server cmd>`), init, grant/revoke, and pack-apply subcommands
 *
 * Phase-0 epic: P0-E7.
 * Every other workspace package is bundled in at publish time (P0-E1-T3), not depended on individually.
 * Placeholder export; real implementation lands as its epic's tasks land.
 */
export const PKG = "knotrust";
