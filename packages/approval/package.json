{
  "name": "@knotrust/approval",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config ../../vitest.config.ts --project approval",
    "lint": "biome check --error-on-warnings ."
  },
  "dependencies": {
    "@knotrust/core": "workspace:*",
    "@knotrust/grants": "workspace:*",
    "@knotrust/store": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/node": "catalog:",
    "vitest": "catalog:",
    "@noble/curves": "catalog:",
    "@knotrust/test-harness": "workspace:*"
  }
}
